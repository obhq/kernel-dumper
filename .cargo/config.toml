[build]
target = "x86_64-unknown-none"
rustflags = ["--cfg", "fw=\"1100\"", "--cfg", "method=\"direct\""]

[unstable]
build-std = ["alloc", "core", "panic_abort"]
build-std-features = ["panic_immediate_abort"]
